<% if @feed_items.any? %>
  <%= will_paginate @feed_items, renderer: FoundationPagination::Rails %>
  <ul class="posts">
    <%= render partial: 'shared/feed_item', collection: @feed_items %>
  </ul>
  <%= will_paginate @feed_items, renderer: FoundationPagination::Rails %>
<% end %>